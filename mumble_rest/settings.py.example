import os
from .base import *

# SECURITY WARNING: keep the secret key used in production secret!
# update this
SECRET_KEY = ""

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = False

# URLS
BASE_URL = ""

ALLOWED_HOSTS = [""]
CSRF_TRUSTED_ORIGINS = [""]

# Ice connectivity
ICE_HOST_HOST = os.environ.get('MURMUR_ICE_HOST', 'localhost')
ICE_HOST_PORT = os.environ.get('MURMUR_ICE_PORT', '6502')
ICE_HOST = 'Meta:tcp -h ' + ICE_HOST_HOST + ' -p ' + ICE_HOST_PORT + ' -t 1000'
ICE_SECRET = os.environ.get('ICE_SECRET', '')
ICE_MESSAGESIZE = 1024 # in KB - Ice default is 1024KB which is 1MB

MUMBLE_ROOT = os.environ.get('MUMBLE_ROOT', os.path.dirname(os.path.abspath(__file__)))
SLICE_FILE = 'MumbleServer.ice'
